export type ActivityParams = Promise<{ activity: string }>
export type UsernameParams = Promise<{ username: string }>
export type StudentParams = Promise<{ student: string }>
export type ActivityStudentParams = Promise<{ student: string; activity: string }>
